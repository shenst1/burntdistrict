$(document).ready ->
  testAlert = (e) ->
    e.preventDefault();
    alert "Make it happen"
  # $(".second").click testAlert

  # $(".second").click pageslide
    # direction: "left"
    # modal: true

